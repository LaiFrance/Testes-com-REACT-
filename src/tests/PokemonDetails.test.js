/* import React from 'react';
import { screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import renderWithRouter from './renderWithRouter';
import App from '../App';

describe('Teste se é renderizado um card com as informações um pokémon.', () => {

test('verifica se A página tem um texto <name> Details que é o nome do Pokémon;', () => {});
test('verifica que Não deve existir o link de navegação para os detalhes do Pokémon selecionado', () => {});
test('verifica se A seção de detalhes deve conter um heading h2 com o texto Summary.', () => {});
test('verifica se A seção de detalhes deve conter um parágrafo com o resumo do Pokémon específico ', () => {});

describe('Teste se existe na página uma seção com os mapas contendo as localizações do pokémon', () => {});

test('Na seção de detalhes deverá existir um heading h2 com o texto Game Locations of <name>', () => {});
test('Todas as localizações do Pokémon devem ser mostradas na seção de detalhes', () => {});
test('Devem ser exibidos, o nome da localização e uma imagem do mapa em cada localização;', () => {});
test('A imagem da localização deve ter um atributo src com a URL da localização;', () => {});
test('A imagem da localização deve ter um atributo alt com o texto <name> location, onde <name> é o nome do Pokémon;', () => {}); */
